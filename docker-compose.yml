version: "3.8"
services:
  bot:
    build: .
    environment: 
      TZ: "Europe/Rome"
  redis:
    image: "redis:alpine"
    volumes:
      - "./redisdata:/data"
    entrypoint: "redis-server --appendonly yes"